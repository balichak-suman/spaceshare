{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Hardhat project for FileLog smart contract",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "hardhat": "^2.20.2",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0"
  }
}
